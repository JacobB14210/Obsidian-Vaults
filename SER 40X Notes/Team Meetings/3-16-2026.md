- Get links and from web scraper into AI API calls
- Have AI analyze links
	- Create report generation based on results 
	- Compare with scoring and comparison
# TODO
- Each municipality is done in a new session
	- Minimize chance of AI straying away from parameters
- Anthropic/Claude for the languages used
- ChatGPT for logic and reasoning
- Go line by line in the rubric and make an assessment
- Example prompt design:

```markdown
system_prompt = """
You are a municipal regulatory analyst for food truck businesses.
You will receive JSON data containing keyword-extracted sentences from a legal document.
Your job is to output a markdown report in EXACTLY this format:
# Summary Report for {filename}
**Overall Score:** {x}%
---
## Overview
| Total Categories | Found | Missing |
...
## Category Status
...
## Key Findings
...
## Recommendations
...
Rules:
- Mark a category as Found only if its keyword_contexts entry has data
- Clean up extracted sentences into readable findings
- Never invent information not present in the data
"""
```

- Passing JSON data for report

```python
import json
with open("Windsor_Food_Trucks.json") as f:
extracted_data = json.load(f)
response = client.messages.create(
model="claude-sonnet-4-6",
max_tokens=4096,
system=system_prompt,
messages=[{
"role": "user",
"content": json.dumps(extracted_data, indent=2)
}]
)
# Save the markdown output
with open("report.md", "w") as f:
f.write(response.content[0].text)
```
